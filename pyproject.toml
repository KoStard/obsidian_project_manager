[project]
name = "obsidian_project_manager"
version = "0.1.0"
dependencies = []
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/obsidian_project_manager"]

[project.scripts]
obsidian_project_manager = "obsidian_project_manager.cli:main"
